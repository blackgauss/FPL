"""FPL: fantasy-premier-league research system.

Layers: `data` (parquet dataset + feature store), `model` (point forecasts +
experiments), `team` (search over squads), `live` (FPL API state + filters).
"""

__version__ = "0.2.0"