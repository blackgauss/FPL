# Pipeline knobs (DVC params).
# A stage using `params:` here invalidates only when a value it reads changes.

seasons: ["2024-2025", "2025-2026"]
split:
  fit_gw_max: 30
  test_start: 31
processed: data/processed
artifacts: experiments/artifacts
pipeline:
  season: 2025-2026
  historical_season: 2024-2025
  gw_start: 31
  gw_end: 33
  fit_gw_max: 30
  test_gw_end: 38
  train_seed: 0
  search:
    enum: greedy
    value: h2h
    n_teams: 20
    seed: 0
    n_samples: 150
  gym:
    top: 3
  experiments:
    parallel: 3
